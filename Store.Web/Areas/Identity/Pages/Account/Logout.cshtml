@page
@model LogoutModel
@{
    ViewData["Title"] = "Выйти из аккаунта";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Нажмите чтобы выйти из аккаунта</button>
            </form>
        }
        else
        {
            <p>Рады были Вас видеть! Возвращайтесь за новыми покупками :)</p>
        }
    }
</header>
